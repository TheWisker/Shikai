'use strict';

var h = require('react').createElement;
var PropTypes = require('prop-types');
var cxs = require('./index');

var Box = function Box(_ref) {
  var _ref$is = _ref.is,
      is = _ref$is === undefined ? 'div' : _ref$is,
      _ref$attr = _ref.attr,
      attr = _ref$attr === undefined ? {} : _ref$attr,
      _ref$css = _ref.css,
      css = _ref$css === undefined ? {} : _ref$css,
      className = _ref.className,
      children = _ref.children;

  attr.className = [className, cxs(css)].join(' ').trim();

  attr['data-hello'] = true;

  return h(is, attr, children);
};

module.exports = Box;